import { StyledSecondaryButton } from "./SecondaryButton.styles";
import { SecondaryButtonProps } from "./SecondaryButton.types";

export const SecondaryButton = (props: SecondaryButtonProps) => <StyledSecondaryButton {...props} />;
